{
  "name": "sheet-api-worker",
  "version": "1.0.0",
  "description": "Google Sheets CRUD API — Cloudflare Worker",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
